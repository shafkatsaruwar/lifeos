diff --git a/node_modules/react-native/scripts/replace-rncore-version.js b/node_modules/react-native/scripts/replace-rncore-version.js
index 1111111..2222222 100644
--- a/node_modules/react-native/scripts/replace-rncore-version.js
+++ b/node_modules/react-native/scripts/replace-rncore-version.js
@@ -33,7 +33,10 @@
 
   if (fileExists) {
     console.log(`Found ${LAST_BUILD_FILENAME} file`);
-    const oldConfiguration = fs.readFileSync(LAST_BUILD_FILENAME).toString();
+    const oldConfiguration = fs
+      .readFileSync(LAST_BUILD_FILENAME)
+      .toString()
+      .trim();
     if (oldConfiguration === configuration) {
       console.log(
         'Same config of the previous build. No need to replace React-Core-prebuilt',
@@ -42,14 +45,8 @@
     }
   }
 
-  // Assumption: if there is no stored last build, we assume that it was build for debug.
-  if (!fileExists && configuration === 'Debug') {
-    console.log(
-      'No previous build detected, but Debug Configuration. No need to replace React-Core-prebuilt',
-    );
-    return false;
-  }
-
+  // pod install extracts the Release core by default. A missing marker must
+  // not skip a Debug swap; otherwise Debug apps link against Release RNCore.
   return true;
 }
 
diff --git a/node_modules/react-native/sdks/hermes-engine/utils/replace_hermes_version.js b/node_modules/react-native/sdks/hermes-engine/utils/replace_hermes_version.js
index 1111111..2222222 100644
--- a/node_modules/react-native/sdks/hermes-engine/utils/replace_hermes_version.js
+++ b/node_modules/react-native/sdks/hermes-engine/utils/replace_hermes_version.js
@@ -33,7 +33,10 @@
 
   if (fileExists) {
     console.log(`Found ${LAST_BUILD_FILENAME} file`);
-    const oldConfiguration = fs.readFileSync(LAST_BUILD_FILENAME).toString();
+    const oldConfiguration = fs
+      .readFileSync(LAST_BUILD_FILENAME)
+      .toString()
+      .trim();
     if (oldConfiguration === configuration) {
       console.log(
         'Same config of the previous build. No need to replace Hermes engine',
@@ -42,14 +45,8 @@
     }
   }
 
-  // Assumption: if there is no stored last build, we assume that it was build for debug.
-  if (!fileExists && configuration === 'Debug') {
-    console.log(
-      'No previous build detected, but Debug Configuration. No need to replace Hermes engine',
-    );
-    return false;
-  }
-
+  // pod install extracts the Release engine by default. A missing marker must
+  // not skip a Debug swap.
   return true;
 }
 
diff --git a/node_modules/react-native/third-party-podspecs/replace_dependencies_version.js b/node_modules/react-native/third-party-podspecs/replace_dependencies_version.js
index 1111111..2222222 100644
--- a/node_modules/react-native/third-party-podspecs/replace_dependencies_version.js
+++ b/node_modules/react-native/third-party-podspecs/replace_dependencies_version.js
@@ -33,7 +33,10 @@
 
   if (fileExists) {
     console.log(`Found ${LAST_BUILD_FILENAME} file`);
-    const oldConfiguration = fs.readFileSync(LAST_BUILD_FILENAME).toString();
+    const oldConfiguration = fs
+      .readFileSync(LAST_BUILD_FILENAME)
+      .toString()
+      .trim();
     if (oldConfiguration === configuration) {
       console.log(
         'Same config of the previous build. No need to replace RNDeps',
@@ -42,14 +45,8 @@
     }
   }
 
-  // Assumption: if there is no stored last build, we assume that it was build for debug.
-  if (!fileExists && configuration === 'Debug') {
-    console.log(
-      'No previous build detected, but Debug Configuration. No need to replace RNDeps',
-    );
-    return false;
-  }
-
+  // pod install extracts the Release deps by default. A missing marker must
+  // not skip a Debug swap.
   return true;
 }
 
