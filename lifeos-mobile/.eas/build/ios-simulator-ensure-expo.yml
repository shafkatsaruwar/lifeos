# iOS simulator build with an explicit local-expo check before prebuild.
build:
  name: iOS simulator build (ensure expo before prebuild)
  steps:
    - eas/checkout
    - eas/use_npm_token
    - eas/install_node_modules
    - run:
        name: Ensure local expo (SDK 54) before prebuild
        command: node ./scripts/ensure-expo-installed.js
    - eas/resolve_build_config
    - eas/prebuild
    - run:
        name: Install pods
        working_directory: ./ios
        command: pod install
    - eas/configure_eas_update:
        inputs:
          throw_if_not_configured: false
    - eas/generate_gymfile_from_template
    - eas/run_fastlane
    - eas/find_and_upload_build_artifacts
